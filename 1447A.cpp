#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main()
{
    ios::sync_with_stdio(0);cin.tie(0);cout.tie(0);
    ll t;
    cin >> t;
    while(t--)
    {
        ll n,i;
        cin >> n;
        cout << n << endl;
        for(i=1;i<=n;i++)cout << i << ' ';
        cout << endl;
    }
}